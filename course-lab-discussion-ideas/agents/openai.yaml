interface:
  display_name: "Course Lab Discussion Ideas"
  short_description: "Build artifact-only discussion ideas from stable interpretation evidence"
  default_prompt: "Use $course-lab-discussion-ideas only after stable interpretation artifacts and parent-passed reference report paths already exist, rely on the local copied scripts in this folder, keep only beyond-handout and non-routine discussion ideas, treat routine comparison, routine uncertainty, ordinary anomaly prose, and normal report obligations as out of scope, read experiment-local permanent memory before deciding whether broad search is allowed, only search or refine novelty-qualified directions that require extra analysis or evidence generation, skip broad first-pass search when permanent memory exists, always run targeted candidate refinement once or twice for retained ideas, write synthesis judgment state such as pending_synthesis_judgment into the handoff artifacts instead of prompting for direct approval here, emit artifact-only JSON and Markdown handoffs, and do not mutate the report or write the final harmonized discussion."
