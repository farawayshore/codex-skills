# Baseline Failure

In one pre-skill attempt to prepare an interference-lab report, the agent skipped a dedicated run-plan stage, drifted into `course-lab-body-scaffold`, and returned only prose notes. No deterministic JSON run-plan contract was produced, so later steps had nothing stable to consume.
